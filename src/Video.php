<?php

/**
 * Video should be rendered as plain text, not a link.
 */
class Video extends MediaBase {
  // // use PostRenderFormatTrait;
  // public function render() {
  //   $url = $this->url;
  //   $label = $this->getLabel();
  //   print "The video $label can be found at $url";
  //  // $this->postRender();
  // }
}